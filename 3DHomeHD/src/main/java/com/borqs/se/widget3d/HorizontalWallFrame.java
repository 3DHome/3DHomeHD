package com.borqs.se.widget3d;

import com.borqs.se.engine.SEScene;

public class HorizontalWallFrame extends WallFrame {

    public HorizontalWallFrame(SEScene scene, String name, int index) {
        super(scene, name, index);
        mWidth = 64;
        mHeight = 43;
        mImageSize = 128;
    }

}
