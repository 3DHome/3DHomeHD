package com.borqs.se.widget3d;

import com.borqs.se.engine.SEScene;

public class VerticalWallFrame extends WallFrame {

    public VerticalWallFrame(SEScene scene, String name, int index) {
        super(scene, name, index);
        mWidth = 43;
        mHeight = 64;
        mImageSize = 128;
    }

}
